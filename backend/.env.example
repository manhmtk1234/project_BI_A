# Environment variables - Database Configuration
DB_HOST=localhost
DB_PORT=3303
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=bi_a_tuananh

# JWT Secret (change in production)
JWT_SECRET=your_secure_jwt_secret_key_here

# Server config
PORT=8080
GIN_MODE=debug

# CORS
FRONTEND_URL=http://localhost:3000

# Example configuration for BI-A Management System
# Copy this file to .env and update with your actual values
# 
# Database Setup:
# 1. Install MariaDB/MySQL
# 2. Create database: bia_management  
# 3. Import schema: mysql -u root -p bia_management < full_database.sql
# 4. Update DB_PASSWORD with your actual password
#
# Security:
# - Use strong password for database
# - Generate secure JWT secret key
# - Never commit .env file to version control
